#include <chrono>
#include <functional>
#include <iostream>
#include <math.h>
#include <memory>
#include <string>
#include <typeindex>
#include <vector>

using namespace std;
using namespace chrono;

template<typename T>
using unowned_ptr = T*;

template<typename T>
using unowned_ptr = T*;